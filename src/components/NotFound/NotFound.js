import React from "react";

import classes from "./NotFound.module.scss"
const NotFound = () => {


  return (
    <div className={classes.pageWrapper}>
      <span className={classes.pageWrapper}>Sorry, this page isn't available.</span>
    </div>
  )
};

export default NotFound;
